# Environment Variables for Vertex AI Service

# Google Cloud Project (Required)
GCP_PROJECT_ID=your_project_id
GCP_LOCATION=asia-northeast3  # Seoul region for Vertex AI

# Optional: Service Account (if not using default credentials)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Service Configuration
SERVICE_NAME=senior-mhealth-ai-simple
PORT=8080

# Logging
LOG_LEVEL=INFO