# Senior MHealth Backend 환경 변수 템플릿
# 이 파일을 .env로 복사하고 실제 값을 입력하세요
# cp .env.template .env

# ============================================================================
# Firebase 프로젝트 설정
# ============================================================================

# Firebase 프로젝트 ID (필수)
# Firebase Console에서 확인: https://console.firebase.google.com
FIREBASE_PROJECT_ID=your-project-id

# Firebase 프로젝트 리전 (필수)
# 권장: asia-northeast3 (서울)
FIREBASE_PROJECT_LOCATION=asia-northeast3

# Firebase Storage 버킷 (선택)
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# ============================================================================
# 개발 환경 설정
# ============================================================================

# 환경 구분 (development | staging | production)
NODE_ENV=development

# 서비스 포트 (로컬 개발용)
PORT=5001

# ============================================================================
# CORS 설정
# ============================================================================

# 허용할 오리진 (쉼표로 구분)
# 예: http://localhost:3000,http://localhost:3001
ALLOWED_ORIGINS=http://localhost:3000

# ============================================================================
# API 보안 설정
# ============================================================================

# API Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15분
RATE_LIMIT_MAX_REQUESTS=100  # 15분당 최대 요청 수

# JWT 설정 (선택 - Firebase Auth 대신 사용할 경우)
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=7d

# ============================================================================
# 데이터베이스 설정
# ============================================================================

# Firestore 컬렉션 접두사 (선택 - 다중 환경 구분용)
FIRESTORE_PREFIX=dev_

# 캐시 설정
CACHE_TTL=3600  # 1시간 (초 단위)

# ============================================================================
# FCM (Firebase Cloud Messaging) 설정
# ============================================================================

# FCM 서버 키 (선택 - 레거시 FCM API 사용 시)
FCM_SERVER_KEY=your-fcm-server-key

# 알림 설정
NOTIFICATION_BATCH_SIZE=500  # 배치 알림 크기
NOTIFICATION_RETRY_COUNT=3   # 재시도 횟수

# ============================================================================
# AI 서비스 연동 (Week 5)
# ============================================================================

# AI 서비스 엔드포인트
AI_SERVICE_URL=http://localhost:8000

# AI API 키 (선택)
AI_API_KEY=your-ai-api-key

# ============================================================================
# 모니터링 및 로깅
# ============================================================================

# 로그 레벨 (debug | info | warning | error)
LOG_LEVEL=info

# 성능 모니터링 활성화
ENABLE_MONITORING=true

# ============================================================================
# 건강 데이터 임계값 (Week 7)
# ============================================================================

# 심박수 임계값
HEART_RATE_MIN=60
HEART_RATE_MAX=100

# 혈압 임계값
BLOOD_PRESSURE_SYSTOLIC_MAX=140
BLOOD_PRESSURE_DIASTOLIC_MAX=90

# 걸음수 목표
DAILY_STEPS_GOAL=5000

# ============================================================================
# 스케줄 설정 (Week 7)
# ============================================================================

# 일일 리포트 생성 시간 (Cron 형식)
DAILY_REPORT_SCHEDULE=0 9 * * *

# 타임존
TIMEZONE=Asia/Seoul

# ============================================================================
# 테스트 설정
# ============================================================================

# 테스트 모드 활성화
TEST_MODE=false

# 테스트 사용자 ID
TEST_USER_ID=test-user-001

# ============================================================================
# 외부 서비스 (선택)
# ============================================================================

# SendGrid (이메일 전송)
SENDGRID_API_KEY=your-sendgrid-key
SENDGRID_FROM_EMAIL=noreply@example.com

# Twilio (SMS 전송)
TWILIO_ACCOUNT_SID=your-account-sid
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_FROM_NUMBER=+1234567890

# ============================================================================
# 주의사항
# ============================================================================

# 1. 이 파일을 Git에 커밋하지 마세요!
# 2. .gitignore에 .env가 포함되어 있는지 확인하세요
# 3. 프로덕션 환경에서는 Firebase Functions Config를 사용하세요:
#    firebase functions:config:set key="value"
# 4. 민감한 정보는 Secret Manager 사용을 권장합니다